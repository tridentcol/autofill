# GitHub Configuration for Auto-Commit Feature
# These are required for admin changes to automatically commit to the repository

# Personal Access Token with repo permissions
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token_here

# Repository owner (username or organization)
GITHUB_OWNER=your_github_username

# Repository name
GITHUB_REPO=autofill

# Branch to commit to (default: main)
GITHUB_BRANCH=main

# Note: On Vercel, GITHUB_OWNER, GITHUB_REPO, and GITHUB_BRANCH
# are automatically set from VERCEL_GIT_* variables if not specified

# Vercel Blob Configuration for Cloud Storage
# Required to upload and store filled documents in the cloud
# Get this token from Vercel Dashboard > Storage > Blob Store

# Read/Write token for Vercel Blob
# Create a Blob store in Vercel and copy the token here
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here
